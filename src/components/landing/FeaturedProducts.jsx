import ProductRail from "./ProductRail";

const FeaturedProducts = (props) => (
  <ProductRail title="Featured Products" subtitle="Fresh picks from trusted sellers" {...props} />
);

export default FeaturedProducts;
